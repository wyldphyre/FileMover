using System;
using System.Linq;
using library;

namespace app
{
  class app
  {
    static void Main(string[] args)
    {
      Console.WriteLine("Hello World!");
      Console.WriteLine("Nothing else implemented yet!");

      // To Do: Start by taking 2 arguments. The first is either a file or folder to process. 
      // The second parameter is the instruction file of what to look for
      // if (args.Any())
      // {
      //   var filename = args.First();
      //   Console.WriteLine(filename);
      // }
    }
  }
}
